/**
 * classe representant le personnage
 */
public class Personnage extends Position {

    /**
     * constructeur qui initialise la position du personnage
     *
     * @param px numéro de ligne
     * @param py numéro de colonne
     */
    public Personnage(int px, int py) {
        super(px, py);
    }
}
