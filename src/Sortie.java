/**
 * classe representant la sortie
 */
public class Sortie extends Position {

    /**
     * constructeur qui initialise la position de la sortie
     *
     * @param px numéro de ligne
     * @param py numéro de colonne
     */
    public Sortie(int px, int py) {
        super(px, py);
    }
}
